# Diabetes-Prediction
A ML project that predicts whether a person is diabetic or not using medical data. The model is built using Python with data pre-processing, feature scaling (StandardScaler), and classification using Support Vector Machine (SVM). It demonstrates an end-to-end ML workflow, including data splitting, training, and evaluation for accurate predictions.
